# Test and tooling dependencies. Versions of the checkers are pinned so CI,
# pre-commit and local runs agree.
-r requirements.txt
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
ruff==0.16.7
mypy==2.3.1
